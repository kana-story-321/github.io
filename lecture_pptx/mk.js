/**
 * mk.js — ワーク生成エンジン
 * 第9回（Lovable LP + 公式LINE 構成設計）を 4ファイル生成。
 *   - ワーク01_基礎_生徒用.pptx
 *   - ワーク01_基礎_講師用.pptx
 *   - ワーク02_応用_生徒用.pptx
 *   - ワーク02_応用_講師用.pptx
 */

const PptxGenJS = require('pptxgenjs');
const wlib = require('./wlib');

function newPres() {
  const p = new PptxGenJS();
  p.layout = 'LAYOUT_WIDE';   // 13.333 x 7.5
  // 上の wlib は 10x5.625 想定なので、レイアウト調整
  p.defineLayout({ name: 'CUSTOM_16_9', width: wlib.SLIDE_W, height: wlib.SLIDE_H });
  p.layout = 'CUSTOM_16_9';
  return p;
}

// ─────────────────────────────────────────────────────────
// ワーク01: 基礎 — Lovable で LP をバイブコーディング
// ─────────────────────────────────────────────────────────
const W01 = {
  cover: {
    title: 'Lovable で LP をバイブコーディング',
    concept: 'AI に頼んで「ブランディング3軸」を反映した LP を世に出す',
  },
  goals: [
    { title: 'AIに頼んで LP を作る',
      detail: 'Lovable に日本語で要件を伝えて、自分の3軸を反映した LP を生成する。' },
    { title: '公開URLまで持っていく',
      detail: '生成→修正→デプロイの流れを実体験し、公開URLとして仕上げる。' },
    { title: '公式LINE登録ボタンを設置',
      detail: 'LP の最後に CTA として公式LINE 登録ボタンを置き、動線の入口を完成。' },
  ],
  terms: [
    { term: 'Lovable',
      desc: 'AIに「こんなWebサイトが欲しい」と日本語で頼むだけでコードを書いてくれる、ノーコード生成ツール。',
      note: 'ブラウザだけで使え、無料枠あり。生成結果は自分のドメインで公開可能' },
    { term: 'LP（ランディングページ）',
      desc: 'SNS や広告から訪れた人が「最初に着地する」1ページのサイト。商談予約や登録の入口にする。',
      note: 'ホームページとの違いは「1つの行動」だけを促す設計のこと' },
    { term: 'バイブコーディング',
      desc: 'コードを書かず、日本語でやりたいことを AI に伝えて修正を重ねながら形にする作り方。',
      note: '空気感(vibe)で指示するスタイル' },
    { term: 'CTA（行動喚起）',
      desc: '「公式LINE登録」「予約する」など、訪問者にして欲しい行動を促すボタンや文言。',
      note: 'Call To Action の略' },
  ],
  ex1: {
    title: '演習1 LP のたたき台を Lovable で生成',
    situation:
      'ブランディング3軸（誰に・何を・どの立ち位置で）は前回整理済み。\n' +
      'これを反映した LP を Lovable に作らせたいが、何をどう伝えれば良いか分からない。',
    target: [
      'Lovable に渡す依頼文(プロンプト)を完成させる',
      '実際に Lovable に投げて、LP のたたき台を1ページ生成する',
      'ファーストビュー(画面上部)に 3軸 が伝わる文言を入れる',
      '最下部に「公式LINEで無料相談」などの CTA ボタンを置く',
    ],
    hint: [
      '依頼文に「ターゲット像」「提供価値」「強み(差別化)」を必ず含める',
      '色味は「3軸のトーン」(信頼感/親しみやすさ など) を指定する',
      '構成は「冒頭→困りごと→解決→ベネフィット→CTA」の順を指示',
      '1ページに収め、文字量は短く、読みやすさ優先',
    ],
  },
  ex1Brush: [
    { title: 'A. ターゲット解像度',
      check: 'プロンプトに「誰の・どんな悩み・どんな状況」が一文で書けたか',
      tip: '「家族の保障を見直したい30代共働き世帯」など、属性+悩み+状況の3要素で書かせる' },
    { title: 'B. 差別化メッセージ',
      check: '同業他社ではなく「私だから選ぶ理由」が伝わる文言になっているか',
      tip: '「3軸の3つ目=立ち位置」を AI に「キャッチコピーに反映して」と追加指示' },
    { title: 'C. CTA の心理ハードル',
      check: '「公式LINE登録」が "次の一歩" に感じられる柔らかい文言か',
      tip: '「無料相談」「資料請求」より「LINEで気軽に質問」など心理ハードルを下げる' },
  ],
  ex2: {
    title: '演習2 LP→公式LINE→面談予約 の動線を設計',
    situation:
      'LP の骨格はできた。次は登録〜面談予約までの動線を AI に整理してもらいたい。\n' +
      'AIDMA(注目→興味→欲求→記憶→行動) に沿って LINE の流れを設計する。',
    target: [
      'AIDMA の5段階別に「LINE で何を見せ、何を聞くか」を1行ずつ書き出す',
      '面談予約までのステップ数を3〜5に絞り、各ステップの目的を明記',
      '公式LINE 登録直後のあいさつ文(自動応答)の文案を1つ作る',
    ],
    hint: [
      '生成 AI に「保険営業の公式LINEを AIDMA で設計して」と頼む',
      '前回の3軸とこの LP の URL を AI に渡してから依頼する',
      '出力された設計をスプレッドシートに表形式で貼り、優先順位を付ける',
    ],
  },
  ex2Brush: [
    { title: 'A. 段階の解像度',
      check: '注目→興味→欲求→記憶→行動 の5段階で「何が違うか」が言えるか',
      tip: '「興味=知りたい / 欲求=自分ごと / 行動=予約する」と言葉で区別させる' },
    { title: 'B. 離脱対策',
      check: '各段階で「離脱しそうな人」へのフォロー文が用意できているか',
      tip: '「迷っている方へ」「もし不安があれば」などの分岐メッセージを AI に出させる' },
    { title: 'C. 行動への橋渡し',
      check: '最後の「行動」段階で、面談予約ページへの導線がはっきりしているか',
      tip: '予約URL を本文に直接埋め、リッチメニューにも同じ予約ボタンを置く' },
  ],
  summary: [
    { head: 'AI が Web 制作の壁を取り払う',
      detail: '日本語で頼むだけで、自分の3軸を反映した LP が今日中に公開できる。' },
    { head: '動線設計には型がある',
      detail: 'AIDMA に沿って LINE を設計すると、登録→面談予約までの流れがブレない。' },
    { head: '完璧より「まず公開」',
      detail: '公開してから修正を重ねるのがバイブコーディング。AI と一緒に磨き上げる。' },
  ],
  // 講師用 解答・指導ポイント
  teacher: {
    ex1Answer: {
      prompt:
        '【貼り付け用】\n' +
        '保険営業向けの LP を作ってください。\n' +
        'ターゲット: 30代共働きで家計と保障の両立に悩む世帯。\n' +
        '提供価値: ライフプラン作成 → 必要な保障の数値化 → 過不足のない提案。\n' +
        '私の立ち位置: 売らない相談相手として伴走する保険プランナー。\n' +
        '構成: 冒頭の共感 → 困りごと → 解決の進め方 → ベネフィット → 公式LINE登録CTA。\n' +
        '色味は信頼感と親しみやすさを両立。1ページで短く読みやすく。',
      output:
        '・ヒーローセクション: 「家計と保障、両立できていますか?」のキャッチ+共感文\n' +
        '・3つの解決ステップ(現状把握→数値化→提案)を1行ずつ\n' +
        '・お客様の声を1〜2件\n' +
        '・最下部 CTA「LINEで気軽に質問する」ボタン',
      guide:
        '・Lovable に投げる前に「3軸シート」を必ず開かせる。空欄が出たら、その場で前回の振り返り。\n' +
        '・生成1回目は必ず微妙。生徒が「失敗だ」と思わないよう、修正を重ねる前提だと事前に伝える。\n' +
        '・CTA文言が硬くなりがち。生徒同士で読み合って「自分なら押すか?」を口に出させる。',
    },
    ex2Answer: {
      prompt:
        '【貼り付け用】\n' +
        '私の公式LINEを AIDMA に沿って設計してください。\n' +
        'LP の URL は ●●● 、ターゲットと3軸は次の通り… (3軸シートを貼る)\n' +
        'AIDMAの各段階で「何を見せるか」「何を聞くか」を表形式で出してください。\n' +
        '面談予約までは3ステップに絞り、各ステップの目的を一文で書いてください。',
      output:
        '・AIDMA別の設計テーブル(5行×3列: 段階/見せるもの/聞くこと)\n' +
        '・自動応答(あいさつ)文案 1案\n' +
        '・面談予約への遷移ステップ(登録 → 価値提示 → 予約案内)',
      guide:
        '・「段階の違いを自分の言葉で説明できるか」を発表させると定着する。\n' +
        '・離脱フォローの分岐を作ると複雑になりがち。最初は1分岐だけに絞らせる。\n' +
        '・面談予約ボタンは LP/LINE 両方に置く。「2回見せる」が原則と強調。',
    },
  },
};

// ─────────────────────────────────────────────────────────
// ワーク02: 応用 — LP→LINE 連携を本番運用へ
//   3 が KPI/テーマ等の知識カード、5〜8 が「場面設定→ゴール」
// ─────────────────────────────────────────────────────────
const W02 = {
  cover: {
    title: 'LP → 公式LINE 連携を本番運用へ',
    concept: 'AIDMA を回して、登録率と予約率を数字で改善する',
  },
  goals: [
    { title: '登録率を計測する',
      detail: 'LP 訪問数→公式LINE 登録の割合を毎週確認できる状態にする。' },
    { title: '配信PDCA を回す',
      detail: '反応率をもとに次回配信を AI と一緒に改善するサイクルを定着させる。' },
    { title: 'リッチメニュー+導線完成',
      detail: '公式LINE のリッチメニューを設置し、LP→LINE→予約 を一本化する。' },
  ],
  // 応用編 STEP1: 知識カード = KPI / 用語
  terms: [
    { term: 'CVR(コンバージョン率)',
      desc: 'LP 訪問者のうち、目的の行動(LINE登録/予約)に進んだ割合。',
      note: '保険LPの目安は1〜5%、低くても3ヶ月の傾向で判断' },
    { term: 'リーチ/開封率',
      desc: 'LINE 配信が「届いた人/読まれた割合」。開封率が低いと配信内容を見直す。',
      note: '読まれない=つまらない の合図' },
    { term: 'A/B テスト',
      desc: '同じ目的に対し2案を出し、反応のよい方を採用する検証手法。',
      note: 'まずは件名や CTA 文言から試すと差が出やすい' },
    { term: 'PDCA',
      desc: 'Plan(計画)→Do(実行)→Check(検証)→Act(改善)を回す業務サイクル。',
      note: 'AIに「Check と Act」を手伝わせると速い' },
  ],
  ex1: {
    title: '演習1 LP の効果を AI で分析',
    situation:
      '【場面】LP公開から2週間。アクセス解析(Google Analytics 等)はあるが、\n' +
      '数字を見てもどう改善すればいいか分からない。',
    target: [
      '【ゴール】過去2週間の LP アクセスデータを AI に渡し、',
      '・現状の CVR と離脱ポイントを言語化',
      '・改善案を「優先度高/中/低」で5つ提示',
      '・来週試す改善1点を決める',
    ],
    hint: [
      'スクショまたはCSV をそのままチャット欄に貼り、「分析して」と頼む',
      '「離脱ポイント」「読了率」「CTA クリック率」の観点を AI に明示',
      '改善案は具体的アクション(変更箇所+文言案)まで書かせる',
    ],
  },
  ex1Brush: [
    { title: 'A. データの渡し方',
      check: '生データだけでなく「目的(=私が知りたいこと)」も伝えているか',
      tip: '「LP 改善の優先順位を決めたい」と先に目的を書いてから貼る' },
    { title: 'B. 比較軸',
      check: '「先週と今週」「LP公開前後」など比較軸を含めて見ているか',
      tip: 'AI に「比較できる軸を3つ提案して」と先に聞いてから分析させる' },
    { title: 'C. 実行可能性',
      check: '改善案が「自分が今週中に1人でできる」レベルに落ちているか',
      tip: '「自分1人で30分以内」の制約を AI に伝えると現実的な案になる' },
  ],
  ex2: {
    title: '演習2 リッチメニュー+導線完成',
    situation:
      '【場面】LP→公式LINE 登録 まではできた。\n' +
      'しかし LINE 内の動線(リッチメニュー)がなく、登録後の次の一歩が示せていない。',
    target: [
      '【ゴール】公式LINE のリッチメニューを設置し、LP→LINE→予約 を一本化',
      '・3軸トーンに合うリッチメニュー画像を AI 生成',
      '・タップ領域に「予約」「資料」「相談」を配置',
      '・テスト配信で意図通り遷移するかを確認',
    ],
    hint: [
      '画像生成は Gemini/Genspark/Canva のいずれか',
      'タップ数は3つに絞り、優先度を「予約>相談>資料」の順に',
      'テストは自分の個人LINE で必ず1往復',
    ],
  },
  ex2Brush: [
    { title: 'A. デザインの一貫性',
      check: 'LP と同じ配色/フォント感で「同じ人の発信」と分かるか',
      tip: '「LP の色コードと同じで作って」と画像生成 AI に明示' },
    { title: 'B. 押したくなる導線',
      check: 'タップ後すぐに「次の一歩」が分かる構成になっているか',
      tip: '予約ボタンを押すと、即「いつが良いか」を聞くメッセージにする' },
    { title: 'C. 計測の仕組み',
      check: '「どのボタンが押されたか」を後で振り返れる仕組みがあるか',
      tip: 'UTMパラメータかタグ機能で「クリック元」を区別して保存させる' },
  ],
  summary: [
    { head: '数字で判断、AIで改善',
      detail: 'CVRと開封率を週1で確認、AIに改善案を出してもらう。' },
    { head: '導線は一本化',
      detail: 'LP→LINE→予約 のどこを切っても「次の一歩」が見えるよう設計。' },
    { head: 'まず3ヶ月続ける',
      detail: 'PDCA は3ヶ月続けないと傾向が見えない。日次は気にしない。' },
  ],
  teacher: {
    ex1Answer: {
      prompt:
        '【貼り付け用】\n' +
        '以下は私のLP(URL: ●●●)のアクセスデータです(過去2週間)。\n' +
        '私の目的は「来週試す改善1点を決める」ことです。\n' +
        '次の観点で分析してください。\n' +
        '  - CVR と離脱ポイント\n' +
        '  - 改善案を優先度(高/中/低)で5つ\n' +
        '  - 各改善案は「変更する箇所」「変更後の文言/レイアウト」を含めて\n' +
        '【データ】(以下に貼る)',
      output:
        '・現状診断: CVR ●%、離脱は「FV→次セクション」で6割\n' +
        '・改善案5件(高=2件、中=2件、低=1件)、各案にビフォー/アフター文言付き\n' +
        '・今週試す1点: ヒーローのキャッチを共感型に変更',
      guide:
        '・「データだけ貼って分析して」と言いがち。必ず "私が知りたいことは" を一文で言わせる。\n' +
        '・改善案が「全部やる」になりやすい。「1週間で1点」を強制すると行動が起きる。\n' +
        '・数字より「文言の手触り」で判断する生徒は、声に出して読ませると気づきが出る。',
    },
    ex2Answer: {
      prompt:
        '【貼り付け用】\n' +
        '公式LINE のリッチメニュー(6分割)用の画像案を作ってください。\n' +
        'LP のメインカラーは ●●●、トーンは「信頼感+親しみ」。\n' +
        '配置: 上段「予約/相談/資料」、下段「お客様の声/プロフィール/お知らせ」。\n' +
        '文字は最小限、アイコン中心、丸み(角丸 R=12px)で。',
      output:
        '・6分割リッチメニュー画像(2500×1686 推奨)\n' +
        '・タップ領域マッピング表(座標と遷移先URL)\n' +
        '・テスト配信用シナリオ(タップ→遷移先メッセージの想定文)',
      guide:
        '・初回生成は文字が多すぎることが多い。「文字は3文字以内/タップ」と再依頼させる。\n' +
        '・タップ領域の座標ズレに注意。LINE 公式アカウントマネージャでプレビュー必須。\n' +
        '・「リッチメニュー作って終わり」になりがち。テスト配信→1往復するまでが課題。',
    },
  },
};

// ─────────────────────────────────────────────────────────
// 生徒用ファイル組立 (9枚)
//   1: 表紙 / 2: 目標 / 3: 用語 / 4: 演習1 / 5: 演習1+ /
//   6: 演習2 / 7: 演習2+ / 8: まとめ
//   ※ 8枚で「8枚=まとめまで」、spec の「9枚」と合うよう先頭に概要は1ページに統合
//   → spec 通り 9 枚: 1表紙/2目標/3用語/4演習1/5演習1+/6演習2/7演習2+/8まとめ(2枚構成)
//   実装: まとめは1枚で、残り1枚は「次回への接続」を入れる方が自然。
//   ここでは仕様通りまず 8 枚で出力、必要に応じ次の指示で増減。
// ─────────────────────────────────────────────────────────
function buildStudent(workDef, isApplied) {
  const p = newPres();
  // 1 表紙
  wlib.coverSlide(p, workDef.cover.title, workDef.cover.concept);
  // 2 学習目標
  wlib.goalsSlide(p, '学習目標', workDef.goals);
  // 3 用語・前提 (応用は KPI/テーマ知識カード)
  const stepTitle = isApplied ? 'STEP1: 抑えておきたい KPI と用語' : '用語と前提';
  wlib.termsSlide(p, stepTitle, workDef.terms);
  // 4 演習1
  wlib.exerciseSlide(p, workDef.ex1.title, workDef.ex1);
  // 5 演習1+
  wlib.brushSlide(p, workDef.ex1.title + ' — 観点別チェック', workDef.ex1Brush);
  // 6 演習2
  wlib.exerciseSlide(p, workDef.ex2.title, workDef.ex2);
  // 7 演習2+
  wlib.brushSlide(p, workDef.ex2.title + ' — 観点別チェック', workDef.ex2Brush);
  // 8 まとめ
  wlib.summarySlide(p, '今日の持ち帰り', workDef.summary);
  return p;
}

function buildTeacher(workDef) {
  const p = newPres();
  wlib.coverSlide(p, workDef.cover.title, workDef.cover.concept, 'teacher');
  wlib.answerSlide(p, workDef.ex1.title, workDef.teacher.ex1Answer);
  wlib.answerSlide(p, workDef.ex2.title, workDef.teacher.ex2Answer);
  return p;
}

// ─────────────────────────────────────────────────────────
// 出力
// ─────────────────────────────────────────────────────────
async function main() {
  const out = require('path').join(__dirname, 'out');
  require('fs').mkdirSync(out, { recursive: true });

  await buildStudent(W01, false).writeFile({ fileName: `${out}/ワーク01_基礎_生徒用.pptx` });
  console.log('✓ ワーク01_基礎_生徒用.pptx');
  await buildTeacher(W01).writeFile({ fileName: `${out}/ワーク01_基礎_講師用.pptx` });
  console.log('✓ ワーク01_基礎_講師用.pptx');
  await buildStudent(W02, true).writeFile({ fileName: `${out}/ワーク02_応用_生徒用.pptx` });
  console.log('✓ ワーク02_応用_生徒用.pptx');
  await buildTeacher(W02).writeFile({ fileName: `${out}/ワーク02_応用_講師用.pptx` });
  console.log('✓ ワーク02_応用_講師用.pptx');
}
main();
